{
  "_comment": "CodeBuddy MCP 配置 - 使用 devPartner 替代所有分散的 MCP 服务",
  "_guide": "将此内容合并到 %USERPROFILE%\\.codebuddy\\mcp.json 中",
  
  "mcpServers": {
    "devpartner": {
      "type": "streamable_http",
      "url": "https://pisces43-dev-partner.ms.show/mcp",
      "_description": "devPartner - 自我进化 MCP 聚合服务 (ModelScope 远程部署)",
      "_setup": "无需本地启动，ModelScope 自动运行。首次配置后重启 CodeBuddy 即可。"
    }
  },
  
  "_migration_note": "如果 devPartner 正常运行，可以移除以下旧的 MCP 服务配置：",
  "_remove_if_devpartner_works": [
    "local-aggregator",
    "filesystem", 
    "sqlite",
    "knowledge-graph",
    "sequentialthinking"
  ],
  
  "_keep_as_backup": {
    "filesystem": {
      "type": "stdio",
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-filesystem", "."]
    }
  }
}
