# DevPartner v7.3.0 - 核心依赖
# ============================
# 合并自:
#   - pyproject.toml (核心依赖)
#   - devpartner_agent/requirements.txt (Agent 层)
#   - devpartner_tools/requirements.txt (工具层)

# MCP 服务框架（必需）
fastmcp>=0.3.0

# HTTP 客户端（用于 Web 请求工具）
httpx>=0.24.0

# YAML 配置解析（必需）
pyyaml>=6.0

# Ollama Python 客户端（可选 — LLM 推理通过 Ollama HTTP API）
# 注意：推理不依赖此包（使用标准库 urllib），仅用于方便管理
# 必须先安装并运行 Ollama 服务：https://ollama.com/download
ollama>=0.4.0