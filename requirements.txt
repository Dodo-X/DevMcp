# DevPartner v6.0 - 核心依赖
# ============================
# 合并自:
#   - pyproject.toml (核心依赖)
#   - devpartner_agent/requirements.txt (Agent 层)
#   - devpartner_tools/requirements.txt (工具层)

# MCP 服务框架（必需）
fastmcp>=0.3.0

# HTTP 客户端（用于 Web 请求工具）
httpx>=0.24.0

# YAML 配置解析（必需）
pyyaml>=6.0

# 本地 LLM 推理引擎（必需 - Qwen3.5-9B 模型推理）
# 安装标准版（CPU）:
#   pip install llama-cpp-python>=0.3.20
#
# 安装 CUDA 加速版（GPU 推荐）:
#   pip install llama-cpp-python --extra-index-url https://abetlen.github.io/llama-cpp-python/whl/cu121
llama-cpp-python>=0.3.20