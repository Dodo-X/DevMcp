[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.backends._legacy:_Backend"

[project]
name = "devpartner"
version = "2.2.0"
description = "DevPartner MCP 服务器 - 合并 tool 和 agent 层"
requires-python = ">=3.10"
dependencies = [
    "fastmcp",
    "httpx",
    "pyyaml",
]
